<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.briefbot.nightly</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__PROJECT_DIR__/briefbot/nightly_briefbot.sh</string>
    </array>

    <!-- Run at 11:59 PM local time. If the machine is asleep at that time,
         launchd will fire the job as soon as it next wakes up. -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>23</integer>
        <key>Minute</key>
        <integer>59</integer>
    </dict>

    <key>RunAtLoad</key>
    <false/>

    <key>WorkingDirectory</key>
    <string>__PROJECT_DIR__</string>

    <!-- Extend PATH so the script finds python3, npm, brew-installed binaries, etc. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <string>__HOME__</string>
    </dict>

    <key>StandardOutPath</key>
    <string>__PROJECT_DIR__/data/logs/launchd.stdout.log</string>

    <key>StandardErrorPath</key>
    <string>__PROJECT_DIR__/data/logs/launchd.stderr.log</string>
</dict>
</plist>
